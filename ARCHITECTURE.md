# Project Structure

- `src/game/`: game loop, physics, bird, and obstacles
- `src/ui/`: DOM user interface
- `styles/`: application styles
- `.github/workflows/`: automated deployment

The code is split by responsibility so gameplay, rendering, UI, and configuration can evolve independently.
