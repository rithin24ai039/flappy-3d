import * as THREE from "https://unpkg.com/three@0.160.0/build/three.module.js";
const SPEED=.055,GAP=3.2;
export function createPipes(scene){const pairs=[];for(let x=6;x<30;x+=7){const y=(Math.random()-.5)*3;const a=make(x,y+GAP/2+4),b=make(x,y-GAP/2-4);pairs.push({a,b,x,y,scored:false});}function make(x,y){const m=new THREE.Mesh(new THREE.BoxGeometry(1.5,8,2),new THREE.MeshStandardMaterial({color:0x32b85c}));m.position.set(x,y,0);scene.add(m);return m;}function update(bird){let hit=false,scored=false;pairs.forEach(p=>{p.a.position.x-=SPEED;p.b.position.x-=SPEED;if(!p.scored&&p.a.position.x<-.8){p.scored=true;scored=true;}if(Math.abs(p.a.position.x)<.9&&(bird.y>p.y+GAP/2-.4||bird.y<p.y-GAP/2+.4))hit=true;});return {hit,scored};}function reset(){pairs.forEach((p,i)=>{p.a.position.x=p.b.position.x=6+i*7;p.scored=false;});}return {update,reset};}